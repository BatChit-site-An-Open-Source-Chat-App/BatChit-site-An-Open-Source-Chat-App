import "../styles/small-spinner.css"; // Import the CSS file for styling

const SmallSpinner = () => {
  return (
    <div className="small-spinner-container">
      <div className="small-spinner"></div>
    </div>
  );
};

export default SmallSpinner;
